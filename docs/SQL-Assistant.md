# SQL Assistant & Smart Templates

Der **SQL Assistant** im Query Builder hilft Ihnen dabei, SQL-Abfragen schneller und fehlerfreier zu erstellen, indem er kontextsensitive Vorlagen anbietet.

## Funktionen

### 🪄 Intelligente Vorlagen
Basierend auf der ausgewählten Tabelle schlägt der Assistent passende Snippets vor:
- **Tabelle untersuchen**: Schnelle `SELECT *` Abfragen mit `LIMIT`.
- **Daten aggregieren**: Vorlagen für `COUNT(*)`, `GROUP BY` und Zeitreihenanalysen.
- **Filterung**: Typische `WHERE`-Klauseln.

### 🧩 Einsetzen per Klick
Snippets können einfach in den Editor eingefügt werden. Wenn Sie Text markiert haben, wird dieser oft intelligent in das Snippet integriert.

### 🏗️ Schema-Bewusstsein
Der Assistent erkennt die Spalten Ihrer Datenquellen und passt die Vorlagen (z.B. für Gruppierungen) automatisch an die verfügbaren Felder an.

## Verwendung
1. Öffnen Sie den **Query Builder**.
2. Wechseln Sie in den **Direct Editor (SQL)** Modus.
3. Klicken Sie auf das **Assistenten-Icon** (🪄) über dem Editor, um die Vorlagenliste zu öffnen.